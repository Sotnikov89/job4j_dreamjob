CREATE TABLE post (
                      id SERIAL PRIMARY KEY,
                      name TEXT,
                      photo_id integer
);
CREATE TABLE candidate (
                      id SERIAL PRIMARY KEY,
                      name TEXT
);